import { Canvas, Meta, Story } from '@storybook/addon-docs';

import { Box } from './Box';

<Meta title="Primitives/Box" component={Box} />

# Box Primitive

The `Box` component, is an HTML Div Element. By default, it has the property `box-sizing: "border-box"`.

<Canvas>
  <Story name="Basic">
    <Box />
  </Story>
</Canvas>
